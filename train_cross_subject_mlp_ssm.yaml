inherits: configs/train_cross_subject.yaml
model:
  decoder_type: mlp
  mlp_hidden_dim: 64
  feature_mode: ssm_only
training:
  learning_rate: 0.0001
  weight_decay: 0.001
  epochs: 10
logging:
  run_name: mlp_ssm_stable